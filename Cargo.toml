[package]
name = "win11-taskbar-autohide"
version = "0.1.0"
edition = "2021"
description = "Toggle Windows taskbar auto-hide using SHAppBarMessage"
license = "MIT"

[dependencies]
windows = { version = "0.56", features = ["Win32_UI_Shell"] }

[build-dependencies]
winres = "0.1"
